<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../../partials/user-head") %>
    <script defer src="/js/show-msg.js"></script>
  </head>
  <body class="bg-veryDarkGray">
    <main class="flex justify-center items-center h-screen w-full">
      <div
        class="bg-white w-96 lg:w-2/5 h-min px-5 lg:px-10 xl:px-20 mx-5 py-5 drop-shadow-[8px_8px_rgb(45,212,191)]"
      >
        <h1 class="text-turquoise text-center font-bold text-3xl">Signup!</h1>
        <form action="/verify-phone" method="post" class="mt-5">
          <div class="mt-2">
            <label for="name" class="block mb-1">Full name</label>
            <input
              type="text"
              required
              id="name"
              name="name"
              class="bg-white border block w-full p-1 px-3 focus:outline-none"
            />
          </div>
          <div class="mt-2">
            <label for="email" class="block mb-1">Email</label>
            <input
              type="email"
              required
              id="email"
              name="email"
              class="bg-white border block w-full p-1 px-3 focus:outline-none"
            />
          </div>
          <div class="mt-2">
            <label for="phone" class="block mb-1">Phone</label>
            <input
              type="tel"
              required
              id="phone"
              name="phone"
              class="bg-white border block w-full p-1 px-3 focus:outline-none"
            />
          </div>
          <div class="mt-2">
            <label for="password" class="block mb-1">Password</label>
            <input
              type="text"
              required
              id="password"
              name="password"
              class="bg-white border block w-full p-1 px-3 focus:outline-none"
            />
          </div>
          <div class="mt-2">
            <label for="confirmPassword" class="block mb-1"
              >Confirm Password</label
            >
            <input
              type="text"
              required
              id="confirmPassword"
              name="confirmPassword"
              class="bg-white border block w-full p-1 px-3 focus:outline-none"
            />
          </div>
          <div class="text-xs mt-3 text-darkGray">
            Password should contain Minimum eight characters, at least one
            uppercase letter, one lowercase letter, one number and one special
            character
          </div>
          <div class="flex justify-center items-center">
            <button
              type="submit"
              class="bg-turquoise mt-5 px-7 py-2 font-semibold uppercase hover:bg-teal-300 transition-all"
            >
              SIGNUP
            </button>
          </div>
          <div class="text-sm text-center mt-2">
            Already have an account?
            <a href="/login" class="text-turquoise hover:underline">Login</a>
          </div>
        </form>
      </div>
    </main>
    <% if(message[0]){ %>
    <!-- errorMsgBox -->
    <div
      id="errorMsgBox"
      class="animate-slide-down transition-all w-full fixed top-4 left-0 right-0 flex items-center justify-center"
    >
      <div
        class="text-center bg-lightRed rounded-lg text-darkRed lg:text-xl py-5 px-8 mx-10 max-w-lg"
      >
        <%= message[0] %>
      </div>
    </div>
    <% } %>
  </body>
</html>
